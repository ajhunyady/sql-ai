Rename testing tab to playground
Playground should show tokens used, LLM duration, total duration

Analyst page should only say "Welcome back" if there are existing conversations, otherwise show "Welcome"
